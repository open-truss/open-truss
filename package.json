{
  "scripts": {
    "lint": "prettier --write \"./**/*.{js,ts,json}\" && oxlint --fix",
    "lint:ci": "prettier --check \"./**/*.{js,ts,json}\" && oxlint",
    "typecheck": "tsc --noEmit -p packages/open-truss && tsc --noEmit -p packages/uqi && tsc --noEmit -p demo-app"
  },
  "devDependencies": {
    "oxlint": "^1.16.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@open-truss/open-truss": "^0.3.0"
  },
  "ot:setup": "open-truss setup"
}
