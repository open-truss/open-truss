workflow:
  version: 1
  signals:
    accountIds: AccountIDs
    userIds: AccountIDs
  frames:
    - frame:
      view:
        component: IncrementAccountID
    - frame:
      view:
        component: IncrementAccountID
    - frame:
      view:
        component: IncrementAccountID
        props:
          accountIds: :userIds
    - frame:
      view:
        component: IncrementAccountID
        props:
          accountIds: :userIds
